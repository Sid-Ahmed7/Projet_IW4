{% extends 'base.html.twig' %}

{% block title %}Plan{% endblock %}

{% block body %}
    <h1>Plan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plan.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ plan.price }}</td>
            </tr>
            <tr>
                <th>Features</th>
                <td>{{ plan.features }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ plan.updatedAt ? plan.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ plan.createdAt ? plan.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ plan.deletedAt ? plan.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ plan.state }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ plan.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plan_index') }}">back to list</a>

    <a href="{{ path('app_plan_edit', {'id': plan.id}) }}">edit</a>
<a href="{{ path('stripe', {'userId': app.user.id, 'planId': plan.id}) }}">Souscrire à ce plan</a>

    {{ include('plan/_delete_form.html.twig') }}
{% endblock %}
