{{ form_start(form) }}
{{ form_widget(form) }}
<div class="flex items-center justify-between">
	<button class="bg-black hover:bg-gray-800 text-white font-bold py-2 px-4 rounded-md mx-auto">{{ button_label|default('Save') }}</button>

</div>
{{ form_end(form) }}
