{% extends 'base.html.twig' %}

{% block title %}Requests{% endblock %}

{% block body %}
    <h1>Requests</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ request.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_requests_index') }}">back to list</a>

    <a href="{{ path('app_requests_edit', {'id': request.id}) }}">edit</a>

    {{ include('requests/_delete_form.html.twig') }}
{% endblock %}
