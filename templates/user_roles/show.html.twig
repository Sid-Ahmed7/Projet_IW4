{% extends 'base.html.twig' %}

{% block title %}UserRoles{% endblock %}

{% block body %}
    <h1>UserRoles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_role.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_role.createdAt ? user_role.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ user_role.deletedAt ? user_role.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ user_role.slug }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_role.updatedAt ? user_role.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_roles_index') }}">back to list</a>

    <a href="{{ path('app_user_roles_edit', {'id': user_role.id}) }}">edit</a>

    {{ include('user_roles/_delete_form.html.twig') }}
{% endblock %}
