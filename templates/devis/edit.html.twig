{% extends 'base.html.twig' %}

{% block title %}Edit Devis
{% endblock %}

{% block body %}

	<div class="flex flex-col bg-fullPink items-center justify-center min-h-screen py-12">
		<div class="bg-white text-xs shadow-md rounded-lg p-8 w-full max-w-md">
			<header class="flex justify-center items-center">
				<img src="{{ asset ('/images/logo2.png') }}" alt="Logo" class="h-12 rounded-full mr-4" width="93">
			</header>


			<div class="flex justify-center items-center mt-4 ">
				<h2 class=" block mb-2 text-xl font-medium ">Modifier le devis</h2>
			</div>
			<br>
			{{ include('devis/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('app_devis_index') }}">back to list</a>
			<a href="{{ path('app_devis_asset_new', {'devisId': devi.id}) }}">

				Ajouter des Options</a>

			{{ include('devis/_delete_form.html.twig') }}


		</div>
	</div>

{% endblock %}
