{% extends 'base.html.twig' %}

{% block title %}Roles{% endblock %}

{% block body %}
    <h1>Roles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Rolename</th>
                <td>{{ role.rolename }}</td>
            </tr>
            <tr>
                <th>RoleDescription</th>
                <td>{{ role.roleDescription }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ role.createdAt ? role.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ role.deletedAt ? role.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ role.updatedAt ? role.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roles_index') }}">back to list</a>

    <a href="{{ path('app_roles_edit', {'id': role.id}) }}">edit</a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
