


{# {% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<div class="flex bg-fullPink flex-col items-center justify-center min-h-screen py-12">
		<div class="bg-white text-xs shadow-md rounded-lg p-8 w-full max-w-md">
			<header class="flex justify-center items-center">
				<img src="{{ asset ('/images/logo2.png') }}" alt="Logo" class="h-12 rounded-full mr-4" width="93">
			</header>
			<br>
			<div class="flex justify-center items-center ">
				<h2 class=" block mb-2 text-sm font-medium ">Inscription</h2>
			</div>
			<br>
			<form action="{{ path('app_register') }}" method="post">
				{% if app.flashes('verify_email_error') %}
					<div class="alert alert-danger">{{ app.flashes('verify_email_error')[0] }}</div>
				{% endif %}

				{% if app.user %}
					<div class="mb-3">
						You are logged in as
						{{ app.user.userIdentifier }},
						<a href="{{ path('app_logout') }}">Logout</a>
					</div>
				{% endif %}
				<div class="mb-4">

					{{ form_row(registrationForm.email) }}
				</div>

				<div class="mb-4">

					{{ form_row(registrationForm.lastname) }}
				</div>

				<div class="mb-4">

					{{ form_row(registrationForm.firstname) }}
				</div>

				<div class="mb-4">

					{{ form_row(registrationForm.username) }}
				</div>

				<div class="mb-4">

					{{ form_row(registrationForm.birthdate) }}
				</div>

				<div class="mb-4">

					{{ form_row(registrationForm.picture) }}
				</div>

				<div class="mb-4">
					{{ form_row(registrationForm.password, {  }) }}
				</div>

				<div class="flex items-center justify-between ">
					<button type="submit" class="bg-purple-600 mx-auto hover:bg-purple-700 text-white font-bold py-2 px-4 rounded-md">S'inscrire</button>
				</div>


			</form>
		</div>
	</div>
{% endblock %} #}

{# templates/registration/register.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.password) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.birthdate) }}
        {{ form_row(registrationForm.picture) }}
        {{ form_row(registrationForm.lastname) }}
        {{ form_row(registrationForm.firstname) }}

        <button type="submit">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
