{% extends 'base.html.twig' %}

{% block title %}Companie{% endblock %}

{% block body %}
    <h1>Companie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ companie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ companie.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ companie.address }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ companie.logo }}</td>
            </tr>
            <tr>
                <th>Banner</th>
                <td>{{ companie.banner }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ companie.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ companie.phoneNumber }}</td>
            </tr>
            <tr>
                <th>TaxNumber</th>
                <td>{{ companie.taxNumber }}</td>
            </tr>
            <tr>
                <th>SiretNumber</th>
                <td>{{ companie.siretNumber }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ companie.likes }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ companie.updatedAt ? companie.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ companie.createdAt ? companie.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ companie.state }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ companie.uuid }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ companie.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_companie_index') }}">back to list</a>

    <a href="{{ path('app_companie_edit', {'id': companie.id}) }}">edit</a>

    {{ include('companie/_delete_form.html.twig') }}
{% endblock %}
