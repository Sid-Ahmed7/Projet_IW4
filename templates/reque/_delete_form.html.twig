<form method="post" action="{{ path('app_reque_delete', {'id': reque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reque.id) }}">
    <button class="btn">Delete</button>
</form>
